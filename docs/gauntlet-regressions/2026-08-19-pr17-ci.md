# PR #17 CI regression note — 2026-08-19

Source workflow run: `32324774745`
PR branch: `fix/gauntlet-0.9.4-model-routing-timing`
PR head: `d3f2ffbd20b77d1dd024d115536f6433f55700d5`
Base: `dca55e52ac75320aa8ca4ab2f70daf3763a69f45`

## Classification

The failed `classify` job reported:

- `PREEXISTING_REGRESSION gauntlet-eval`
- `PR_REGRESSION test`
- `PREEXISTING_REGRESSION test-browser`

The `test` classification is not supported by the PR diff. The PR changes Gauntlet routing/docs/contracts and does not modify `tests/architecture/core-ts-isolation.test.ts`, toolchain pinning, or `tests/capture-wip.node.test.ts`.

Base and head both failed the `test` check for environment/timing-related failures. The head run additionally timed out in `tests/architecture/core-ts-isolation.test.ts`, which changed the failure signature and caused the classifier to label the whole check `PR_REGRESSION`.

## Pre-existing failures to pick up

### gauntlet-eval

Both base and head fail with:

`FAIL acceptance persistence rejects blank screenshots — missing: manifest.evidence.screenshots`

### test-browser

Both base and head fail with the same signature:

- `BROWSER-3V3-MATCH`: Vite `server.fs` access denied for `docs/screenshots/BROWSER-3V3-MATCH/frame-before.png`
- `BROWSER-5V5-MATCH`: Vite `server.fs` access denied for `docs/screenshots/BROWSER-5V5-MATCH/frame-000.png`

### test / CI classification robustness

Base already fails:

- mise Node/pnpm toolchain checks (`mise exec -- node --version`, `mise exec -- pnpm --version`)
- `tests/capture-wip.node.test.ts` hook timeout

Head had those same failures plus a timeout in `tests/architecture/core-ts-isolation.test.ts`. Because the classifier compares the whole check signature, this transient extra timeout is currently surfaced as a PR regression.

## Requested handling

Do not change gameplay or accepted evidence from PR #17 to mask these failures. Treat the items above as Gauntlet/CI regression work and address them independently from the 0.9.4 model-routing PR.
