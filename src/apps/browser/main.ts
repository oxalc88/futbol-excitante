/** Browser composition root — imports the placeholder from contracts as a smoke entry. */
import { placeholder as foundation } from "../../simulation/config/foundation";

console.log("PES Simulator loaded", foundation);